<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apply for Scholarship - Smart Scheme</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3a0ca3;
            --accent-color: #f72585;
            --light-gray: #f8f9fa;
            --medium-gray: #e9ecef;
            --dark-gray: #495057;
            --white: #ffffff;
            --success-color: #4cc9f0;
            --warning-color: #f8961e;
            --error-color: #ef233c;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f5f7ff;
            color: #333;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            margin-bottom: 30px;
            border-bottom: 1px solid var(--medium-gray);
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            color: var(--secondary-color);
        }

        .logo span {
            color: var(--primary-color);
        }

        .nav-buttons {
            display: flex;
            gap: 15px;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 6px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            font-size: 14px;
        }

        .btn-primary {
            background-color: var(--primary-color);
            color: var(--white);
        }

        .btn-primary:hover {
            background-color: var(--secondary-color);
        }

        .btn-outline {
            background-color: transparent;
            border: 1px solid var(--primary-color);
            color: var(--primary-color);
        }

        .btn-outline:hover {
            background-color: var(--primary-color);
            color: var(--white);
        }

        .form-container {
            background: var(--white);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            margin-bottom: 40px;
        }

        .form-header {
            margin-bottom: 30px;
            text-align: center;
        }

        .form-header h1 {
            color: var(--secondary-color);
            font-size: 28px;
            margin-bottom: 10px;
        }

        .form-header p {
            color: var(--dark-gray);
            font-size: 16px;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        fieldset {
            border: 1px solid var(--medium-gray);
            border-radius: 8px;
            padding: 25px;
            background-color: var(--light-gray);
        }

        legend {
            font-size: 18px;
            font-weight: 600;
            color: var(--secondary-color);
            padding: 0 15px;
            background-color: var(--white);
            border: 1px solid var(--medium-gray);
            border-radius: 6px;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-group label {
            font-weight: 500;
            color: var(--dark-gray);
            font-size: 14px;
        }

        .form-group label.required::after {
            content: " *";
            color: var(--error-color);
        }

        .form-control {
            padding: 12px 15px;
            border: 1px solid var(--medium-gray);
            border-radius: 6px;
            font-size: 15px;
            transition: all 0.3s ease;
            background-color: var(--white);
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.2);
        }

        textarea.form-control {
            min-height: 120px;
            resize: vertical;
        }

        .file-upload {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .file-upload-item {
            display: flex;
            flex-direction: column;
            gap: 5px;
        }

        .file-upload-label {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 500;
        }

        .file-upload-label .icon {
            color: var(--primary-color);
            font-size: 18px;
        }

        .file-input {
            display: none;
        }

        .file-custom {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 15px;
            background-color: var(--white);
            border: 1px dashed var(--medium-gray);
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .file-custom:hover {
            border-color: var(--primary-color);
            background-color: rgba(67, 97, 238, 0.05);
        }

        .file-custom .btn {
            padding: 5px 10px;
            font-size: 12px;
        }

        .file-name {
            font-size: 13px;
            color: var(--dark-gray);
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .form-actions {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-top: 20px;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background-color: var(--medium-gray);
            border-radius: 4px;
            margin-bottom: 30px;
            overflow: hidden;
        }

        .progress {
            height: 100%;
            background-color: var(--success-color);
            width: 25%;
            transition: width 0.3s ease;
        }

        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }
            
            header {
                flex-direction: column;
                gap: 15px;
            }
            
            .nav-buttons {
                width: 100%;
                justify-content: space-between;
            }
            
            .btn {
                width: 48%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo"><span>SMART</span> SCHEME</div>
            <div class="nav-buttons">
                <a href="/profile"><button class="btn btn-outline">Back to Profile</button></a>
                <a href="/logout"><button class="btn btn-primary">Sign Out</button></a>
            </div>
        </header>

        <main>
            <div class="progress-bar">
                <div class="progress"></div>
            </div>

            <div class="form-container">
                <div class="form-header">
                    <h1>Apply for {{scholarship.name}}</h1>
                    <p>Please fill out the form completely and accurately. All fields marked with * are required.</p>
                </div>

                <form action="/submit-application" method="POST" enctype="multipart/form-data">
                    <!-- Personal Details -->
                    <fieldset>
                        <legend>Personal Information</legend>
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="firstName" class="required">First Name</label>
                                <input type="text" id="firstName" name="firstName" class="form-control" value="{{user.first_name}}" required>
                            </div>
                            <div class="form-group">
                                <label for="lastName" class="required">Last Name</label>
                                <input type="text" id="lastName" name="lastName" class="form-control" value="{{user.last_name}}" required>
                            </div>
                            <div class="form-group">
                                <label for="dob" class="required">Date of Birth</label>
                                <input type="date" id="dob" name="dob" class="form-control" value="{{user.date_of_birth}}" required>
                            </div>
                            <div class="form-group">
                                <label for="fatherName" class="required">Father's Name</label>
                                <input type="text" id="fatherName" name="fatherName" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label for="motherName" class="required">Mother's Name</label>
                                <input type="text" id="motherName" name="motherName" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label for="fatherOccupation" class="required">Father's Occupation</label>
                                <input type="text" id="fatherOccupation" name="fatherOccupation" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label for="motherOccupation" class="required">Mother's Occupation</label>
                                <input type="text" id="motherOccupation" name="motherOccupation" class="form-control" required>
                            </div>
                        </div>
                    </fieldset>

                    <!-- Income Details -->
                    <fieldset>
                        <legend>Financial Information</legend>
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="annualIncome" class="required">Annual Income (INR)</label>
                                <input type="number" id="annualIncome" name="annualIncome" class="form-control" value="{{user.annual_income}}" required>
                            </div>
                            <div class="form-group">
                                <label for="aadharNumber" class="required">Aadhar Number</label>
                                <input type="text" id="aadharNumber" name="aadharNumber" class="form-control" value="{{user.aadhar}}" required>
                            </div>
                        </div>
                    </fieldset>

                    <!-- Academic Details -->
                    <fieldset>
                        <legend>Academic Information</legend>
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="courseType" class="required">Course Type</label>
                                <input type="text" id="courseType" name="courseType" class="form-control" value="{{user.course_type}}" required>
                            </div>
                            <div class="form-group">
                                <label for="academicYear" class="required">Academic Year</label>
                                <input type="text" id="academicYear" name="academicYear" class="form-control" value="{{user.academic_year}}" required>
                            </div>
                            <div class="form-group">
                                <label for="percentageMarks" class="required">Percentage / Marks</label>
                                <input type="number" id="percentageMarks" name="percentageMarks" class="form-control" value="{{user.percentage_or_marks}}" required>
                            </div>
                            <div class="form-group">
                                <label for="attendancePercentage" class="required">Attendance Percentage</label>
                                <input type="number" id="attendancePercentage" name="attendancePercentage" class="form-control" value="{{user.attendance_percentage}}" required>
                            </div>
                            <div class="form-group">
                                <label for="qualificationExamScore" class="required">Qualification Exam Score</label>
                                <input type="number" id="qualificationExamScore" name="qualificationExamScore" class="form-control" value="{{user.qualification_exam_score}}" required>
                            </div>
                        </div>
                    </fieldset>

                    <!-- Bank Details -->
                    <fieldset>
                        <legend>Bank Account Information</legend>
                        <div class="form-grid">
                            <div class="form-group">
                                <label for="bankName" class="required">Bank Name</label>
                                <input type="text" id="bankName" name="bankName" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label for="accountNumber" class="required">Account Number</label>
                                <input type="text" id="accountNumber" name="accountNumber" class="form-control" required>
                            </div>
                            <div class="form-group">
                                <label for="ifscCode" class="required">IFSC Code</label>
                                <input type="text" id="ifscCode" name="ifscCode" class="form-control" required>
                            </div>
                        </div>
                    </fieldset>

                    <!-- Document Upload -->
                    <fieldset>
                        <legend>Document Upload</legend>
                        <div class="file-upload">
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">📷</span>
                                    Passport Size Photo (Max 2MB)
                                </label>
                                <input type="file" id="photo" name="photo" class="file-input" accept="image/*" required>
                                <label for="photo" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                            
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">📄</span>
                                    Income Certificate (PDF/JPEG/PNG, Max 5MB)
                                </label>
                                <input type="file" id="incomeCertificate" name="incomeCertificate" class="file-input" accept=".pdf,.jpg,.jpeg,.png" required>
                                <label for="incomeCertificate" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                            
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">🆔</span>
                                    Aadhar Card (PDF/JPEG/PNG, Max 5MB)
                                </label>
                                <input type="file" id="aadharCard" name="aadharCard" class="file-input" accept=".pdf,.jpg,.jpeg,.png" required>
                                <label for="aadharCard" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                            
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">📝</span>
                                    Mark Sheet (PDF/JPEG/PNG, Max 5MB)
                                </label>
                                <input type="file" id="markSheet" name="markSheet" class="file-input" accept=".pdf,.jpg,.jpeg,.png" required>
                                <label for="markSheet" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                            
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">🏷️</span>
                                    Category Certificate (PDF/JPEG/PNG, Max 5MB)
                                </label>
                                <input type="file" id="categoryCertificate" name="categoryCertificate" class="file-input" accept=".pdf,.jpg,.jpeg,.png" required>
                                <label for="categoryCertificate" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                            
                            <div class="file-upload-item">
                                <label class="file-upload-label required">
                                    <span class="icon">🏦</span>
                                    Bank Passbook (PDF/JPEG/PNG, Max 5MB)
                                </label>
                                <input type="file" id="bankPassbook" name="bankPassbook" class="file-input" accept=".pdf,.jpg,.jpeg,.png" required>
                                <label for="bankPassbook" class="file-custom">
                                    <button type="button" class="btn btn-outline">Choose File</button>
                                    <span class="file-name">No file chosen</span>
                                </label>
                            </div>
                        </div>
                    </fieldset>

                    <div class="form-actions">
                        <button type="button" class="btn btn-outline" onclick="window.location.href='/profile'">Cancel</button>
                        <button type="submit" class="btn btn-primary">Submit Application</button>
                    </div>
                </form>
            </div>
        </main>
    </div>

    <script>
        // Update file name display when a file is selected
        document.querySelectorAll('.file-input').forEach(input => {
            input.addEventListener('change', function() {
                const fileName = this.files[0] ? this.files[0].name : 'No file chosen';
                this.nextElementSibling.querySelector('.file-name').textContent = fileName;
            });
        });

        // Simple form progress indicator
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.querySelector('form');
            const progressBar = document.querySelector('.progress');
            
            form.addEventListener('input', function() {
                const totalFields = form.querySelectorAll('input[required], select[required], textarea[required]').length;
                const filledFields = Array.from(form.querySelectorAll('input[required], select[required], textarea[required]'))
                    .filter(field => field.value.trim() !== '').length;
                
                const percentage = Math.min(100, (filledFields / totalFields) * 100);
                progressBar.style.width = percentage + '%';
            });
        });
    </script>
</body>
</html>